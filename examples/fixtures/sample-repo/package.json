{
  "name": "taskflow-api",
  "version": "0.3.1",
  "private": true,
  "description": "HTTP API that turns external task events into recorded approval decisions.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "start": "node --experimental-strip-types src/index.ts"
  },
  "dependencies": {
    "express": "^4.19.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
